<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:layout_gravity="center_horizontal"
    android:gravity="center"
    android:id="@+id/home_screen_layout"
    tools:context="com.example.clayto.rssreaderapp.HomeScreenActivity">

    <Button
        android:layout_width="300dp"
        android:layout_height="100dp"
        android:gravity="center"
        android:text="@string/new_articles"
        android:id="@+id/live_feeds_button"
        android:textSize="20dp"
        android:onClick="viewLiveFeeds"/>

    <Button
        android:layout_marginTop="20dp"
        android:layout_width="300dp"
        android:layout_height="100dp"
        android:gravity="center"
        android:text="@string/saved_articles"
        android:textSize="20dp"
        android:id="@+id/saved_feeds_button"
        android:onClick="viewSavedFeeds"/>

</LinearLayout>
